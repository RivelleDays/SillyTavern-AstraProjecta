Drawer-mapped workspace.

- No Astra view renderer; the main area ports the SillyTavern `WorldInfo` drawer when the nav tab is active.
- Keep IDs aligned with `NAVIGATION_SECTIONS.top` (`id: 'world-info'`).
