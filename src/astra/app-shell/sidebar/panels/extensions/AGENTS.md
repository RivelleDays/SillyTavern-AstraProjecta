Stub panel for Extensions.

- Sidebar panel intentionally empty; main area surfaces the SillyTavern `rm_extensions_block` drawer.
- Keep IDs/labels aligned with `NAVIGATION_SECTIONS.top` (`id: 'extensions'`).
