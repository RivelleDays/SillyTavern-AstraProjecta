export function renderExtensionsPanel(container) {
    if (!(container instanceof HTMLElement)) return
    container.replaceChildren()
    // Drawer-backed view; content is surfaced via main-area drawer mapping.
}
